[package]
name = "audit-tools"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "audit-tools"
path = "src/main.rs"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
clap = { version = "4", features = ["derive"] }